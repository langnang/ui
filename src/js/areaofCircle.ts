/**
 * @name areaOfCircle
 * @desc 返回圆的面积
 * @param {Number} radius 
 * @param {Number} pi 
 */
export const areaofCircle = (radius: number, pi: number = Math.PI) => radius * radius * pi
export const __areaofCircle__ = {
  description: "圆的面积"
}
